<!--pages/labDetail/labDetail.wxml-->
<!-- 引入 WXS -->
<wxs src="../../utils.wxs" module="utils" />

<view class="lab-detail-container">

  <view class="lab-card">

    <view class="info-item">
      <text class="info-label">实验室名称：</text>
      <text class="info-value">{{labArr.laboratory_name || '未设置'}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">实验室地点：</text>
      <text class="info-value">{{labArr.location || '未设置'}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">实验室描述：</text>
      <text class="info-value">{{labArr.description || '暂无描述'}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">实验室状态：</text>
      <text class="info-value">{{labArr.status || '未知'}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">借用人：</text>
      <text class="info-value">{{username}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">用途：</text>
      <text class="info-value">{{reservationArr.purpose}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">状态：</text>
      <text class="info-value">{{reservationArr.status}}</text>
    </view>

    <view class="info-item">
      <text class="info-label">预订使用时间为：</text>
      <text class="info-value">{{utils.formatDateTime(reservationArr.start_time)}} 至 {{utils.formatDateTime(reservationArr.end_time)}}</text>
    </view>

    <view class="info-item" wx:if="{{reservationArr.return_at}}">
      <text class="info-label">归还时间：</text>
      <text class="info-value">{{utils.formatDateTime(reservationArr.return_at)}}</text>
    </view>
  </view>

  <view class="action-area">
    <button wx:if="{{reservationArr.status == '未完成'}}" class="cancel-btn" bindtap="cancelReservation">强制取消预约</button>
  </view>

  <!-- 自定义底部导航栏 -->
  <custom-tabbar current="4"></custom-tabbar>

  <!-- 给底部栏留出空间 -->
  <view class="tabbar-placeholder"></view>
</view>